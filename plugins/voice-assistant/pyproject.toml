[project]
name = "voice-assistant"
version = "0.1.0"
description = "Voice input/output plugin for Claude Code"
requires-python = ">=3.10"
dependencies = [
    "claude-agent-sdk>=0.1.0",
]

[tool.uv]
dev-dependencies = []
